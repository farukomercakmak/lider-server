#!/bin/sh

set -e

if [ -d /usr/share/lider-server-old ]; then

    cp -rf /usr/share/lider-server-old/etc/tr.org.liderahenk.cfg  /usr/share/lider-server/etc/
    cp -rf /usr/share/lider-server-old/etc/tr.org.liderahenk.datasource.cfg  /usr/share/lider-server/etc/
fi
/etc/init.d/lider start
